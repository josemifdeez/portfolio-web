---
// src/components/StudiesItem.astro
interface Props {
  title: string
  company: string
  date: string
}

const { title, company, date } = Astro.props
---

<div
  class="relative mx-12 pb-4 grid before:absolute before:left-[-35px] before:block before:h-[100%] before:border-l-2 before:border-black/20 dark:before:border-white/15 before:content-[''] md:grid-cols-5 md:gap-0 md:space-x-4"
>
  <div class="relative pb-12 md:col-span-10"> {/* Ensure this container allows content to grow vertically */}
    <div class="sticky top-0"> {/* Be cautious with sticky positioning inside changing height elements */}
      <span class="text-yellow-400 -left-[42px] absolute rounded-full text-5xl">•</span>
      
      {/* ---- THIS IS THE MODIFIED LINE ---- */}
      <h3 class="text-xl font-bold text-yellow-400">{title}</h3> 
      {/* ---- END OF MODIFIED LINE ---- */}

      <h4 class="font-semibold text-xl text-gray-600 dark:text-white">{company}</h4>
      <time class="p-0 m-0 text-sm text-gray-600/80 dark:text-white/80">{date}</time>
    </div>
  </div>
</div>